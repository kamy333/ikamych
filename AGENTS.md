# Project Instructions

- Default local site URL: `http://ikamy.local/public/index.php`
- When opening or testing the app in a browser, use `http://ikamy.local` as the local base URL unless the user gives a different path.
- Existing smoke test command: `.\scripts\smoke.ps1 -BaseUrl "http://ikamy.local"`
